<Window x:Class="KumanoKodo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Kumano Kodo" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header and dummy comment -->
        <TextBlock Grid.Row="0" 
                   Text="Kumano Kodo Japanese Learning"
                   FontSize="24" 
                   FontWeight="Bold"
                   Margin="20,20,20,10"
                   HorizontalAlignment="Center"/>

        <!-- Navigation Menu -->
        <StackPanel Grid.Row="1" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top">
            <Button Content="Vocabulary" 
                    Margin="10" 
                    Padding="20,10"
                    Style="{StaticResource NavigationButton}"/>
            <Button Content="Grammar" 
                    Margin="10" 
                    Padding="20,10"
                    Style="{StaticResource NavigationButton}"/>
            <Button Content="Practice" 
                    Margin="10" 
                    Padding="20,10"
                    Style="{StaticResource NavigationButton}"/>
        </StackPanel>
    </Grid>
</Window> 